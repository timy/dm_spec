#ifndef ENERGY_H_
#define ENERGY_H_

void para_energy_ini( struct config_t* cfg, struct parameters *ps );
void para_energy_del( struct parameters *para );
void set_para_energy( struct parameters *para );

#endif
